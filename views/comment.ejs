<div class="card-body">
    <from >
    <%for (let a in post.postComment){%>
    <p class="card-text">

        <%= post.postComment[a].comment %>
    </p>
        <div class="btn-group">
          <button type="button" class="btn btn-sm btn-outline-secondary"><a href="/comment/remove/<%= post.postComment[a]._id %>" style= "text-decoration:none;">Delete</a></button>
        </div>
    <%}%>
    </from>
    <div class="d-flex justify-content-between align-items-center">
        <div class="btn-group">
        </div>
    </div>
</div>